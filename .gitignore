# Capacitor Android shell build artifacts — the actual app source lives in
# the project root; these are all generated/copied.
node_modules/
www/
android/app/build/
android/app/debug/
android/app/release/
android/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/build/
android/.idea/

# Cloudflare Pages Functions local dev state (wrangler pages dev)
.wrangler/

# Local secrets for `wrangler dev`/`wrangler pages dev` (e.g. OPENCHARGEMAP_API_KEY,
# TOMTOM_API_KEY) — real secrets belong in `wrangler secret put` / the Pages
# dashboard, never committed here even for local testing.
.dev.vars

.DS_Store
