# Capacitor Android shell build artifacts — the actual app source lives in
# the project root; these are all generated/copied.
node_modules/
www/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/build/

# Local secrets for testing Cloudflare Pages Functions (wrangler pages dev) —
# holds a real/throwaway TOMTOM_API_KEY for local runs only, never committed.
.dev.vars
