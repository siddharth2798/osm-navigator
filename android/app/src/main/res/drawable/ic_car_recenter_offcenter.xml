<?xml version="1.0" encoding="utf-8"?>
<!-- Shown instead of ic_car_recenter while the map has been manually panned
     away from the puck (NavigationScreen.followModeActive) — same
     navigation-arrow glyph as the phone's own locateBtnIcon(offCenter=true)
     in app.js, for the same "an icon swap, not just a recolor, is easy to
     miss while driving" reasoning. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="48dp"
    android:height="48dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
  <path
      android:pathData="M12 2 L19 21 L12 17 L5 21 Z"
      android:fillColor="#FFFFFF" />
</vector>
