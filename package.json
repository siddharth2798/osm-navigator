{
  "name": "osm-navigator",
  "version": "1.0.0",
  "private": true,
  "description": "Personal turn-by-turn navigation PWA — this file exists only to support the optional Capacitor native Android shell. The web app itself has no build step; open index.html directly or serve the folder statically.",
  "scripts": {
    "cap:copy": "rm -rf www && mkdir -p www && cp index.html app.js config.js idb.js native-location.js style.css manifest.json sw.js www/ && cp -r icons www/icons && cp -r vendor www/vendor",
    "cap:sync": "npm run cap:copy && cap sync android"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
